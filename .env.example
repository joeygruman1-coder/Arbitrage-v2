# Optional. Public exchange catalogs and built-in matching need no keys.
# This key enables conservative AI review of the first 30 links.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Optional
PORT=3000

# Continuous full-catalog scanner defaults
SCAN_INTERVAL_MS=300000
