# Optional. The app works without a key using its built-in matching engine.
# Add an OpenAI API key to enable a second-pass AI review of likely pairs.
OPENAI_API_KEY=

# Optional
PORT=3000

# The scanner runs continuously. Defaults shown below.
SCAN_INTERVAL_MS=120000
MAX_POLYMARKET_PAGES=500
MAX_KALSHI_PAGES=100
